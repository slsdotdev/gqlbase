{
  "name": "@gqlbase/example",
  "description": "Example project for gqlbase, demonstrating how to use the core library and CLI to transform GraphQL schemas.",
  "version": "0.0.1",
  "main": "src/index.ts",
  "type": "module",
  "private": true,
  "sideEffects": false,
  "scripts": {
    "codegen": "gqlbase --verbose",
    "watch": "gqlbase --watch --verbose"
  },
  "devDependencies": {
    "@gqlbase/cli": "^0.0.10"
  }
}
